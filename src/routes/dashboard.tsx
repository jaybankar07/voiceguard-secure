import { createFileRoute } from "@tanstack/react-router";
import { DashboardPage } from "@/components/dashboard/DashboardPage";
export const Route = createFileRoute("/dashboard")({ head: () => ({ meta: [{ title: "Dashboard — VoiceGuard" }, { name: "description", content: "VoiceGuard security operations dashboard." }, { property: "og:title", content: "Dashboard — VoiceGuard" }, { property: "og:description", content: "VoiceGuard security operations dashboard." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: DashboardPage });